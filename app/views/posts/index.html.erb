<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<!-- <table class = "table table-hover">
  <thead>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th>Blog entry</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.author %></td>
        <td><%= post.blog_entry %></td>
        <td><%= link_to 'Show', post, class: "btn btn-primary" %></td>
        <td><%= link_to 'Edit', edit_post_path(post), class: "btn btn-warning" %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->
<% @posts.each do |post| %>

    <div class = "col-md-4">
      <div class="panel panel-primary">
        <div class="panel-heading">
          <h3 class="panel-title"><%= post.title %><span style = "float:right;">By: <%= post.user.username %></span></h3>
        </div>
        <div class="panel-body">
          <%= post.blog_entry %>

          <br>
          <hr>
          <tr>
            <td><%= link_to 'Show', post, class: "btn btn-primary" %></td>
            <td><%= link_to 'Edit', edit_post_path(post), class: "btn btn-warning" %></td>
            <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
          </tr>
        </div>
      </div>
    </div>


<% end %>

<br>

<%= link_to 'New Post', new_post_path, class: "btn btn-primary" %>
